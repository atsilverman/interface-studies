# Server-only secrets. Copy to `.env` locally. Never prefix with VITE_.
# VITE_* is baked into the browser bundle and is public.
#
# Create a user API key: https://cursor.com/dashboard/api
# Paste the value only in `.env` (local) or the host's env UI (Vercel / GitHub).

CURSOR_API_KEY=
